#ifndef ps2_h
#define ps2_h
#include "stdbool.h"

void large_and_small(const int*, int, int*, int*);
double inner_product(const double* leftArray, const double* right_array, int length);
bool compareArrays(int*,int,int*,int);

#endif

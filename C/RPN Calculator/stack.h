#ifndef stack_h
#define stack_h
#include "node.h"

void push(node*); //push a node to the stack
node* pop(void);//pop a node off the stack
node* peek(void);//look at top node of the stack
void clearStack(void);//clear the stack


#endif
